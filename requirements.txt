# --- Core framework ---
Django>=6.0,<7.0

# --- Forms & images ---
Pillow>=12.0
django-crispy-forms>=2.6
crispy-bootstrap5>=2026.0

# --- PDF / QR ---
reportlab>=4.4
qrcode>=8.0

# --- Static files in production ---
whitenoise>=6.10

# --- WSGI server ---
gunicorn>=23.0

# --- Env management ---
python-dotenv>=1.0

# --- Database ---
# SQLite is built into Python. When you switch to Postgres, uncomment:
# psycopg[binary]>=3.2

# --- Error tracking (optional: only activates when SENTRY_DSN is set) ---
sentry-sdk>=2.20

# --- Optional deployment deps ---
# dj-database-url>=2.2       # parse DATABASE_URL env var
